# Otomemo（旧称 creative_manager＝ローカルdir/パッケージ名は据置・GitHubは otomemo）— 開発ルール

実装は **SDD（スペック駆動）＋TDD** で進める。スペックが正準、コードは従う。

## ブランチ運用＝単一ブランチ（main）
- **ソロ開発なのでブランチは分けない。作業も push も `main` 直**（2026-07-07 統一）。
- 理由：分割は無益・工数増・取り違えでミスの元。feature/workspace ブランチは作らない。

## スペックの所在（上ほど安定・権威）
- コンセプト＋要件: `docs/requirements.md`
- アーキテクチャ: `docs/architecture.md`
- 設計(SDD): `docs/design.md`
- タスク: Task機能（#1〜）
- テスト / コード

## 積みタスク（やりそびれ・保留）の在り処
- **`docs/backlog.md`** ＝「いつかやる／後回し」の置き場（undo/redo 版管理・死にコード撤去・systemd 自動起動・
  worker の claude_prompt 完全撤去・UX磨き 等）。着手したら Task 化して backlog からは消す。新しい保留もここへ。

## ドキュメントの置き場＝状態で決める（2026-07-29 再編）
doc の状態は3つ。**置き場が状態を表す＝冒頭に「状態＝◯◯」の自己申告は書かない。**
- **正準（今の真実）**＝`docs/` 直下（requirements / architecture / design / backlog / process 等）。現況主義＝古い記述は上書き・削除してよい（過去は git 履歴）。
- **仕掛かり（検討中・裁定待ち）**＝`docs/drafts/`。**設計案・計画・たたき台はここに作る（research/ に置かない）**。索引＝`drafts/README.md`（裁定待ちリスト・**空が健全**）。裁定が出たら（採用でも却下でも）要点を正準へ昇格記載し、本文は `docs/archive/` へ移動して archive 索引に決着1行。
- **アーカイブ（済んだ経緯・根拠）**＝消さない・上書きしない：
  - **事実の記録**（外部調査・理論・実測・監査）→ `docs/research/`（従来どおり・README に1行）
  - **それ以外の済んだ doc**（決着した設計案・消化済み計画・引き継ぎ・振り返り・戦略検討）→ `docs/archive/`（README に1行）
- 判定の一文テスト：**「〜である」（事実）＝research／「〜すべき」「この順で」（案・段取り）＝drafts→archive**。迷ったらオーナーに1行で聞く。
- 引き継ぎ書はアーク単位で drafts/ に1本（生きている間は上書き可）。アーク完走で archive/ へ。セッションの進行管理そのものは Task／backlog が正準。
- 経緯：2026-07-29 に research/ から済んだ非研究 doc 約40本を `docs/archive/` へ移送済み（リンク全張替え・検証済み）。**進行中アーク（作詞補助・パターンライブラリ）の doc は完走時に遷移**＝それまで research/ に残っていてよい。

## 研究・調査タスクの出力（ルール）
- **外部調査・理論研究・分析・実測の結果は必ず `docs/research/` 配下に `.md` で格納する**（会話に流して消さない）。
  例：`motif-extraction.md`・`melody-heuristics.md`・`self-check-log.md`。出典(URL)・実測値・設計含意を残す。
- **索引＝`docs/research/README.md`**（マスター目次・到達点サマリ＋全docのグループ別一覧）。**新規 research doc を作ったら README に1行追加**＝散逸防止。
- 外部研究は**出典付き**（Webなら引用URL）。**他者コーパス(POP909等)からは統計のみ抽出**＝リテラルな旋律/モチーフは保存しない（著作権）。自作ネタは可。

## レイヤーの正準順序
**コンセプト → アーキテクチャ → 要件 → 設計 → タスク → テスト → コード**
- 上ほど抽象・安定・権威。下ほど具体・流動。上位が下位の根拠。
- （要件↔アーキは入れ替え可。"何を"を先に見たい時は要件を上に置く）

## 修正時のルール（最重要）
既存を変えるときは、**上から下へ整合を確認してから**変える：
1. コンセプト/体験原則に反しないか
2. アーキテクチャに反しないか
3. 要件に反しないか
4. 設計(SDD)に反しないか
5. タスクに反映する
6. テストを先に書く（赤）
7. コードを変える（緑 → リファクタ）

鉄則：**変更は上位から下ろす。コードだけ変えて上位と乖離させない。** 乖離を見つけたら「上位を意識的に更新する」か「変更を捨てる」のどちらか。後追いでスペックを腐らせない。

## SDD
- 実装はスペックに従う。食い違ったら **スペックを先に直してからコード**。
- 設計変更は `design.md` に先に書く。

## TDD
- データ構造・契約（スキーマ / ジョブ / MIDI入出力 / トランスポーズ / 検索）は **テスト先行**。ここが命。
- 赤 → 緑 → リファクタ。

## strictness は段階的
- **序盤（探索・最初の縦スライス）**：緩くてよい。動かして学ぶ優先、テスト後追いも可。
- **コア・契約・修正フェーズ**：上の確認順序＋TDDを厳守。
- 実装は **縦スライス**（UI＋API＋ドメインを一緒に薄く）。横積みしない。

## 進め方
- 節目（一段落）ごとに残タスクを出す（忘れ防止）。勝手に「終わり」を宣言しない。
