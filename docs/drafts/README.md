# drafts/ 索引 — 検討中・裁定待ち（仕掛かり）

**定義**：まだ真実でない doc ＝ 設計案・計画・たたき台。**ここにあること自体が「未裁定」の宣言**（冒頭のステータス自己申告は書かない）。
**遷移**：裁定が出たら（採用でも却下でも）要点を正準（design.md 等）へ昇格記載し、本文は `../archive/` へ移動して archive 索引に決着1行。**この一覧が空であることが健全**。
**規約**：新規の設計案・計画はここに作る（research/ に置かない）。詳細は CLAUDE.md「ドキュメントの置き場」。

## 裁定待ちリスト
- [2026-07-23-pattern-picker-demotion-plan](2026-07-23-pattern-picker-demotion-plan.md) — **「パターンを選ぶ」帯の格下げ計画（オーナー方針・実装なし・別アーク）**。指示＝一等地占有の帯を「ライブラリから読み込む」的な二次アクションへ降格。中身はTask2/L3で既にライブラリ検索化済＝**見せ方/配置/文言だけ**を落とす（候補取得/適用ロジック不変＝bit一致は構造保証）。案A（設定行の二次リンク化・器PatternPickerBarに`variant`追加で丸ごと再利用）＋案C（空グリッドのゴーストCTA）併用を**推奨**／案B（⋯メニュー沈め＝最強格下げだが新規メニュー器要）。文言＝「ライブラリから読み込む」（一般語）。触るのは`PatternPickerBar.tsx`＋呼び出し3箇所（Chord/Bass/Rhythmエディタ）・L3の`fetchPatterns`は不触。地雷=aria-labelをテストが握る/はみ出しクランプ流用/モバイル390幅折返し。未決7点（推奨案可否・主文言・展開の形・案C要否・Section扱い・M5順序）
- [2026-07-30-lyric-editor-screen-decisions](2026-07-30-lyric-editor-screen-decisions.md) — **残るのは件1だけ＝詞モード（1音符ずつの直し）の見せ方**（2026-07-30b 更新・作詞補助アーク・design §31-11 の16）。**件2/件3/件4 は決着＝実装済み**（オーナー不在中に推しで進めた＝**未レビュー・1件ずつ戻せる**）＝件2 案C（未反映のときだけ反映ボタン）・件3 案B（韻律トグルは奥へ＋OFFを覚える）・件4 文言一式（字足らずN／まだ反映していません／まだ読みを取っていません／かなを消す）。実測＝歌詞まわりの常設 約220px→**52px**・ロールが折り返し内（上端261/下端743）。**件1が残る理由＝推し（案B ボタンに言葉を出す）の前提が「オーナーが実機で3分触ること」で代われない**（押す前に用が分かったか／1音直したいとき探しに来るか）。モード行の言葉「詞」も未変更。以下は元の材料。**件1 詞モードの見せ方**＝A据置／**B ボタンに言葉を出す（推し）**／Cパネルへ移す／D入口2つ／E廃止。事実＝説明が `title` 属性だけ＝スマホでは一度も出ない（`KindEditorBody.tsx:188`）・利用ログの仕組みが無く「使われていない」証拠は取れていない＝**実機3分の確認手順を併記**（押す前に用が分かったか／1音直したいとき探しに来るか）。文脈行（2行バー）は今は足さない。**件2 歌詞1行の見た目**＝Aチップ1個／B音数も／**C 未反映のときだけ反映ボタン（推し）**／Dテキストだけ。事実＝表記を1文字直すと控えが古くなりチップが消える（`lyric.ts:316-318`→`PianoRoll.tsx:293,308,425`）＝手が要る状態こそ画面が空白になる。**件3 韻律トグル**＝A奥へ／**B 奥へ＋OFFを覚える（推し）**／C常設維持／D廃止。事実＝`showFit` は `useState(true)`（`PianoRoll.tsx:95`）で**閉じると必ずONに戻る**＝奥へ移すだけだと毎回2タップ。**件4 文言**＝現行/案1/案2 の対比表3枚（状態チップ・読みの様子・操作）。推し＝**「あとN音」→「字足らずN」**（字余りと対の詩歌の語）・**未反映チップを新設「まだ反映していません」**（ボタン「読みを反映」と動詞をそろえる）・「読みを取ります」→「まだ読みを取っていません」・「クリア」→「かなを消す」・モード行「詞」→「1音ずつ」、他は据え置き（「読みを反映」は裁定済みで動かさない）。文言変更の費用＝テストが文字を握るのは2行だけ（`PianoRoll-lyric.test.tsx:315,323`）。音符を割る操作の言葉は**存否が研究の監査待ち**＝作らない結論なら行ごと消える。決まると design §31-9/§31-11 を確定版へ書き換え→歌詞パネル実装＋かな欄「流し込む」の退役（同時実施と決めてある）
