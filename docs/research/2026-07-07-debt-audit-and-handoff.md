# 負債監査＋開発引き継ぎ（Opus向けハンドオフ・2026-07-07）

**目的**：本リポジトリの「修整したほうがいい負債と修整方針」「不足機能」「今後実装すべき機能」を1枚に統合し、
別セッション（Opus）が **この doc → 各正典 doc** の順で読めば作業を引き受けられる状態にする。
**詳細設計・実装手順には降りない**＝各項目の詳細は担当セッションが正典を読み、CLAUDE.md のルール
（上位から下ろす・TDD・research doc 格納）に従って詰めること。

**正典の所在**（本 doc は索引・監査であり正典ではない）：
- 機能ロードマップの正典 = `docs/research/2026-07-07-next-dev-plan.md`（P0〜P4計画・25項目ギャップ表）
- 保留タスクの正典 = `docs/backlog.md`
- 実装現況の正準 = `docs/status.md`／設計決定ログ = `docs/design.md`

---

## 0. 現状スナップショット（2026-07-07時点）

- **構成**：2プロセス＝api(:8787 Fastify+MCP+web配信+ジョブ消化・TS) ＋ cm-search(:8788 意味検索・残る唯一のPython)。
  worker（旧Pythonジョブワーカー）は撤去済。脳=常駐Claudeクライアント、ドメイン能力=creative-manager MCP。
- **規模**：api 66ファイル/約9.7k LOC、web 52ファイル/約13.4k LOC、worker(cm-search) 約250 LOC。
- **健全性**：tsconfig strict＋noUncheckedIndexedAccess、TODO/FIXMEコメント0件、apiの音楽エンジンはテスト厚い
  （api 66・web 40＋e2e 19ファイル）。全体としてコード衛生は良好＝負債は「散らかり」でなく「偏り」型。
- **ブランチ**：**単一ブランチ運用（main）に統一（2026-07-07）**。ソロ開発でブランチ分割は無益・工数増・取り違えの元、というオーナー判断で `project-workspace`／`melody-engine-eval-harness` は main へ畳んで廃止。以後は main 直で進める。
- **直近の到達点**：P0完了（調内音ハイライト／evalMelody露出／句割り基盤／doc是正）＋P1（自己進化ループ常設）まで。

---

## 1. 修整したほうがいい負債と修整方針

優先度順。各項目は「負債 → 方針」の1セット。詳細は担当セッションで詰める。

### 1-1. 高：品質リスクが実利用に直結するもの

| # | 負債 | 修整方針 |
|---|------|---------|
| D1 | **`apps/web/src/audio.ts`（665行）が無テスト＋`any` 31件集中**。再生＝毎回使う中核なのにリスクが二重 | ✅**完了（2026-07-07）**：純関数（`drumKey`/`velToMidi`抽出/`drumDetune`/`drumNameFor`/`resolveSF2Ctor`/`preset*`）を切り出し `audio.test.ts`(24)追加。`preset*`の`any`3件を`PresetLike`型で撤去、Tone/smplr由来のリスクある`any`は残置。挙動不変（式は移動のみ）・web 40ファイル311緑・tsc緑 |
| D2 | ~~**`apps/api/src/reaper.ts`（414行）が無テスト**。常駐Claudeプロセスのライフサイクル制御~~ | ✅**完了＋監査是正（2026-07-07）**：実コード確認で監査の前提が誤りと判明＝`reaper.ts` は**プロセス制御ではなくジョブ結果→ネタ化の消費者**で、materialize分岐は `job.test.ts`(28)＋audio-analyze/research-runner/midi-import で**既に厚くテスト済**。未カバーだった純粋ヘルパの境界（フラット/ダブル臨時記号キー・frame検証）を直接テスト化＝`keyToPc`/`frameVals`/`hasMusic` を export し `reaper-helpers.test.ts`(19)を追加。**常駐Claudeプロセス制御は別ファイル（reaperではない）** |
| D3 | **音楽定数・理論ロジックの api↔web 二重実装**（PITCH_NAMES/KEY_NAMESが同一配列を別名で二重定義、コード品質テーブル、transpose系）。片方だけ直すと挙動がズレる | ✅**完了（2026-07-07）**：`packages/music-core`（`@cm/music-core`）を新設し不変知識（PITCH_NAMES/QUALITY_INTERVALS〔34品質・移設前に完全一致を実証〕/normRoot/chordPcs）を SSOT化。api `theory.ts` は re-export で import 面不変、web `music.ts` は重複リテラル撤去。design 決定2b に記載。music-core 10＋api 559＋web 311 緑・両tsc緑。相対bass/生成器等アプリ固有は共有せず結合最小 |
| D4 | **docs間の乖離**（§2に列挙）。SDDの権威が腐る最短経路 | ✅**完了（2026-07-07）**：§2の5件＋追加発見1件を是正済（コード変更なし・doc編集のみ） |

### 1-2. 中：開発速度・保守性に効くもの

| # | 負債 | 修整方針 |
|---|------|---------|
| D5 | **Lint不在**（ESLint/フォーマッタ設定が一切ない）。strict TSが最後の砦になっている | ESLint flat config＋最小ルール（未使用import・no-explicit-any警告程度）を導入し、既存コードは一括autofix＋手動0にはこだわらない。CIがないので `pnpm lint` をテストと並ぶ習慣に |
| D6 | **UI巨大コンポーネント**：`App.tsx`（957行）・`SectionEditor.tsx`（908行）。backlog既載（SectionEditor分割=リファクタ大） | 挙動不変の機械的分割（SectionEditor→PlacePicker等の抽出はbacklogに分割案あり。SongStatus分離が前例）。App.tsx は画面遷移状態をhooks/子に降ろす。**e2eが19本あるので分割後の回帰確認に使う** |
| D7 | **`apps/api/_feas_*.ts` 6本（約490行）の死にコード**。ビルド外だが誤読ノイズ | ✅**完了（2026-07-07）**：6本削除（参照ゼロを grep で確認）。知見は `docs/research/` に既存 |
| D8 | **systemd 死にユニット**：`deploy/systemd/` に撤去済み `cm-worker.service` と廃止済み `cm-music-mcp` 参照が残り、install.sh が実行体不在のユニットを入れようとする（backlog 2026-07-07摘出済） | ✅**完了（2026-07-07）**：`cm-worker.service` 削除＋install.sh の cm-worker 参照2箇所除去。生存＝cm-api/cm-search/cm-backup |
| D9 | **systemd 自動起動が未インストール**＝母艦再起動でスタック落ち・手起動（backlog既載・ユーザー方針「最後でよい」） | `--user` enable を実施。ユーザー方針を尊重しつつ D8 とセットなら安い |

### 1-3. 低：気づいたら拾う程度

- `apps/worker` の命名と実体（cm-search専用）の不一致 → リネームは参照更新が広いので、まず pyproject の description と README 記述の整合だけでも可。
- `_dogfood_ui/` の未追跡ファイル（Playwrightスクショツール＋PNG群）→ .gitignore に追加 or `tools/` へ整理。
- ~~`pnpm-workspace.yaml` の `packages/*` glob 空振り~~ → ✅D3 で解消（`packages/music-core` が入った）。
- backlog「片付け」節・「ドッグフード由来」節の小物（kindColor SSOT・childDur共通化・絵文字→SVG・検索0件ヒント等）→ backlog参照。
- ~~**`project-workspace` → main のマージ**（5コミット滞留）~~ → **解消済（2026-07-07）**：単一ブランチ運用へ統一し main に畳んだ（§0参照）。

### 修整時の共通ルール（CLAUDE.mdより・Opusへの申し送り）

1. 変更は上位から下ろす（コンセプト→アーキ→要件→設計→タスク→テスト→コード）。設計変更は design.md に先に書く。
2. 契約・データ構造はテスト先行（赤→緑→リファクタ）。
3. backlog の項目に着手したら Task 化して backlog から消す（or `→ #NN`）。
4. 分析・実測は `docs/research/` に doc を残し README 索引に1行追加。
5. フルe2eは毎回回さない（コスト重）。ユニット＋ピンポイントで確認。

---

## 2. docs間乖離の是正リスト（D4の内訳）→ ✅**全5件是正済（2026-07-07）**

いずれも doc 編集のみで潰せた。是正先は「実態に合わせる」方向。**D4は完了＝本節は履歴として残す**：

1. ✅ **`docs/usecases-chat.md` 冒頭ヘッダ**が「定義のみ・実装未着手」のまま → 「✅全4実装済・本番live＋①アナリーゼv1」に更新済。
2. ✅ **`docs/requirements.md` 本文**（L182/L193）にパッドステップ入力が残存 → 撤去注記（2026-07-04・ロール一本化）を本文2箇所に追記済。
3. ✅ **`docs/design.md` 決定1**の「追い抜き完了までPythonドメイン(cm-music-mcp:8790)を消さない」条件付き記述 → S2で廃止完了・2プロセスの旨、完了注記を追記済。
4. ✅ **MCPの旧ジョブ系ツール**：design.md 棚卸し「#101でCUT宣言したが並存（撤去未了）」→ **legacy維持を正式決定（chat面は非公開・full面はtest互換で当面残す／コード撤去はbacklog）**として design.md に明記済。
5. ✅ **`docs/research/2026-07-07-next-dev-plan.md`** の「worker移植が残る」系の乖離指摘 → P0-d(commit 2bc0e8c)で他docは是正済＝計画doc側の指摘自体が解消の旨、是正済み注記を追記済。
6. ✅ **（監査中に追加発見）`docs/status.md` L72** が「3プロセス／worker ヘッドレス live」のまま（同doc L24は撤去済と矛盾）→ 「2プロセス・worker撤去済・systemd未インストール」に是正済。

---

## 3. 不足機能リスト（実装済みの土台に対する穴）

正典＝backlog.md＋next-dev-plan.md ギャップ表。ここではグループと優先度だけ（詳細・実装案は正典側に既にある）：

### A. メロの書きやすさ・質（最優先領域＝ユーザーの苦手×設計思想「候補を出す」）
- メロ生成 brush-up ①〜⑦（interruption2分割／装飾を型に＝最重要／強拍suspension／Narmour閾値／head選定／句機能変奏／16分細分）— 理論裏打ち済・**耳検証が要るので音が聴ける環境で着手**
- 和声/終止 brush-up ①〜⑩（終止タイプ選択器／偽終止／声部進行減点／avoid-note／二次ドミナント／サブドミナントマイナー等）
- メロ補完の6/8作り込み・局所infill/変奏拡張
- ベロシティ/強弱編集（今は一律100・humanizeの土台）／メトロノーム・カウントイン／跳ね・スイング（「跳ねるボタン」）／アルペジエーター

### B. 一曲に仕上げる動線
- 曲フォーム組み立て（section/song箱はあるが薄い＝タイムライン・リピート・セクション再利用）
- 簡易ミックス（音量/ミュート/ソロ）
- 音声(WAV/mp3)書き出し（今はMIDIのみ）／MusicXML書き出し（入力のみ・仮歌への橋）

### C. データ・可逆性の土台
- ネタの版管理（revise/assembleのサーバ側undo/redo・設計から起こす）
- エディタUndoの残り（section/songコンテナ対応・トースト）／ノート編集v2（範囲マーキー・ドラッグ移動）

### D. 器（プロジェクト）まわりの残り
- [M] ファイル追加・プレビュー（紐付け先ネタ選択が要設計）／[L] プロジェクト改名・削除／[L] 既定会話の整合／[L] 指示の即時反映

### E. 運用
- quota（7日）監視（常駐ClaudeがMax認証＝枯渇で会話が死ぬ・検知＋表示）
- systemd自動起動（→負債D8/D9）
- chat-liveバッファ肥大化・孤児スレッド剪定（低）

---

## 4. 今後実装すべき機能（ロードマップ＝next-dev-plan.md の現在地更新）

next-dev-plan の P0〜P4 が正典。**P0は完了、P1は本体実装済**（コミット `6fa4320`）まで進んだのが現在地。残るアーク：

1. **P1残り**：自己進化ループの配線の仕上げ・実利用での検証（多様候補がchatから実際に効く形になっているかの確認）。
2. **P0-b残り（深い方）**：フレーズ対称/非対称の**V2非対称本実装**（アウフタクト・句の伸縮・問い4+答え4）。ユーザー『対称も非対称も選べないとダメ』＝必須フィーチャー。
3. **P2（キーストーン）**：自作mp3の資産化＝自作曲→analyze_audio→study(本人)→個人プロファイル→検索/ランクを自作寄せにバイアス。「自分らしさ」の燃料であり、コンセプト（延長として働く・好みに寄せる）への直結度が最も高い。
4. **P3**：メロ層を操作面に（骨格固定・表面N案／変奏ファミリー／対照検索）。※作業時は【骨格】/【表面】どちらを触るか毎回明記（ユーザールール）。
5. **P4+**：曲全体足場（構成/転調/対旋律）／仮歌（VOICEVOX・実測済）＋WAV書き出し／版管理undo／情報収集パーソナライズ。
6. **通奏低音**：歌詞韻律（アクセント輪郭→メロ制約）の小実験。アナリーゼv1.1の残（構成機械抽出・コード訂正UI等）も usecases-chat.md 参照。

**アーク選択（P2に進むか、P1/P0-bの仕上げを先にするか）はオーナー判断**＝next-dev-plan の「未決＝オーナーの選択」がまだ生きている。着手前に確認すること。

---

## 5. Opusへの作業受け渡し（推奨の切り出し方）

独立に走らせられる粒度で並べる（依存が薄い順）：

1. ✅ **doc是正パック**（§2の5件＋追加1件）— **完了（2026-07-07・D4クローズ）**。
2. **片付けパック**（D7 死にコード削除＋D8 systemdユニット剪定＋_dogfood_ui整理）— 低リスク。
3. **テスト補強パック**（D1 audio.ts／D2 reaper.ts）— 挙動変更なし・テストのみ先行。
4. **music-core切り出し**（D3）— design.md に決定を書いてから。D1と順序調整（audio.tsを触る前後どちらでやるか）。
5. **Lint導入**（D5）— 独立。大量autofixのdiffが出るので単独コミットに。
6. **機能アーク**（§4）— オーナーのアーク選択を確認してから Task 化して着手。

各パック共通：着手時に backlog/Task の突き合わせ → 完了時に status.md・本doc・backlog を更新。
「問題が無ければ止めない」「一段落ごとに残タスク提示」「報告はステートレス（毎回文脈再掲）」がオーナーの進め方ルール。
