# メロディ生成 研究仕様（フレーズ感・息継ぎ／連想記憶への道）

最終更新: 2026-06-23 ／ 種別: **研究→仕様（実装前・レビュー用）**。3視点パネル（理論→決定的ルール／計算手法サーベイ／メロ連想・エッセンス）を統合。
位置づけ: これは「非常に大きな機能（メロの連想記憶）」の**1個目**。北極星＝**参照メロ（例：椎名林檎）のエッセンスを引いて"違うメロ"を生成**。

## 0. 現状診断（なぜ「呼吸しない」のか）
`apps/api/src/music/generate.ts` の `genMelody` は「1小節モチーフを全小節に反復＋拍頭コードトーン拘束＋軽い変奏」。良い土台だが、**phrase/period(2小節/4小節)の上位構造・句末の休符/長音・終止の安定音着地・頂点(クライマックス)設計がいずれも構造的に欠落**＝延々続いて息継ぎしない。原因はモチーフ展開でなく**上位の"呼吸の骨格"が無い**こと。

## 0.5 ユーザー確定（2026-06-23）
- **拍子を最初から設計に**：4/4 だけでなく **6/8（ユーザー嗜好）・3/4 等を一級**で扱う。フレーズ/息継ぎ/グルーヴは拍子で変わる前提。
- **モチーフ＝1〜2小節**。骨格は **2 / 4 / 8 小節**にスケール。4・8小節は**モチーフ2つ（A/B）**の構成もあり（sentence/period や A-A-B-A 的）。
- **連想（S4/S5）は別トラック**：メロ"書法"（骨格・呼吸＝S1-3）と、メロ"連想記憶"（エッセンス抽出→違うメロ＝S4-5）は別物として進める。
- **著作権＝抽象のみ**：メロは具体（著作権物）を持つと死ぬ。エッセンスは抽象/相対/統計層だけ（§3-4・LCS上限）。
- **骨格の難所は要・追加深掘り**（下記 §9）：弱起、**コード音でない所から滑り込む**（強拍の非和声音→解決＝アポジャトゥーラ的な表情）、拍子別グルーヴ。文献が薄い領域＝理論＋一次原理＋実例で詰める。

## 1. 確定した設計原則（3視点が収束）
1. **骨格優先（skeleton-first）**：音を作る前に**フレーズ構造＝呼吸を先に置く**。`motive(1小節)→phrase(2小節)→period(4小節=問い/答え)`。各句末に**休符 or 長音（息継ぎ）**、終止で**安定音(1/3/5・period末は主音)へ着地**、**頂点を後半≈0.62**に1つ。＝WuYun/GTTM の「骨格を先に作り装飾で埋める」を**学習なし・記号的に**模す。
2. **決定的・ルール＋制約・既存資産を壊さない**：#86 どおり LLM は音符に触らない。手法は **CSP的規則（音域/跳躍上限/跳躍後は順次反行/輪郭ターゲット/句末closure）＋モチーフ変換（反復/模続/反転/拡大縮小/断片化）**。純マルコフ単独（無限に続く・コーパス必須）とニューラル（重い・非決定）は**採らない**（知見源に留める）。コーパスがあれば**弱マルコフを"制約を満たす範囲のバイアス"**として後付け（無くても規則だけで回る＝degrade gracefully）。
3. **骨格＝エッセンス（連想記憶との統一）**：生成の「骨格」と、参照メロから抽出する「エッセンス」は**同じ表現**。参照メロを骨格抽出（GTTM time-span 還元の軽量版）→**別の進行/別の肉付けで再構築**＝「似て非なるメロ」。生成と連想が1つの骨格概念で繋がる。
4. **著作権はコードより一段慎重**（メロは最も保護される層）：エッセンスは**抽象/相対/統計の層のみ**保持（音程列・輪郭・リズム指紋・度数/PC分布・非和声音の使い方）。**絶対ピッチ＋絶対リズムの生メロ全曲は保持/配布しない**。生成出力は**元曲との最長共通一致(LCS)を上限制約**で断つ。「度数列＝権利対象外」の整理はメロでも**抽象層なら通る見込み**だが運用で担保。

## 2. 推奨アーキテクチャ（3層・決定的）
```
[骨格層] phrase/period を割り当て→句末に休符＋安定音着地→頂点(≈0.62)を配置（呼吸を先に作る）
   ↓
[制約層] 骨格間を装飾で埋める：コードトーン拘束(既存)＋CSP規則(音域/跳躍/跳躍後反行/非和声音は順次解決)
   ↓
[変奏層] 既存のモチーフ反復＋変換(模続/反転/拡大縮小/断片化)を**句機能で位置駆動**に（consequent=模続、句末=拡大/断片化）
```
既存 `genMelody`（リズム図形＋度数コントゥア＋モチーフ反復）は**変奏層そのもの**＝捨てずに上に骨格層を載せるのが最小改修・最大効果。

## 3. エッセンス表現（連想記憶の材料・著作権セーフ度つき）
層を連結した「メロ指紋」で運用（コード進行の指紋ベクトルと同型）。
- **E1 輪郭(Parsons/5段階)**：身振り。最も安全・識別力低。
- **E2 音程列(移調不変)**：旋律骨格・"らしさ"の主力。**既存 `melodySimilarity` がこれ**。高安全。
- **E3 リズム指紋(IOI比/拍位置/シンコペ率)**：ノリ。高安全。←今の類似度が無視してる重要層。
- **E4 度数/PC分布**：旋法・多用する度数。高安全。
- **E5 非和声音の使い方(経過/刺繍/掛留/アポジャトゥーラの癖)**：個性の核。**既存 `analyze_fit` が材料**。高安全。
- **E7 フレーズ構造(反復/対句・自己相似)**：骨組み。SIATEC/COSIATEC or n-gram反復検出。
- **E8 絶対ピッチ＋絶対リズムの同時一致**＝**複製＝使わない/避ける対象**（コピー検出の上限）。
「らしさ」＝統計/相対の層（E2/E3/E4/E5）。固有表現＝E8（具体の長い一致）。

## 4. エッセンス→"違うメロ"生成（似て非なるの作り方）
**factorize & recombine**（保持する層／作り直す層を選ぶ）：
- G1 輪郭+リズム保持・音程作り直し（フレーズの寄せ）
- G2 リズム保持・ピッチ再サンプル
- **G3 統計(E2/E4/E5)を種に新規**（"癖"だけ継ぐ＝最も連想らしい）＋**G4 モチーフ変換で展開**（似て非なるの古典解）
いずれも**コードトーン拘束＋analyze_fit 適合**で合法な音に閉じる。
**境界＝二重しきい値**：`θ_lo ≤ 参照との類似 ≤ θ_hi`（下限=エッセンス保持／上限=コピー回避）＋**絶対ピッチ＋リズムの LCS が n音以上一致したら棄却**（"originality report" 方式・法的にも安全側）。決定的・TDD固定可。

## 5. メロコーパスの現実解
- **Hooktheory/TheoryTab(HLSD)型**＝U-FRET 取込の完全な対応物：**調基準の度数で human-transcribed の melody+chord・注釈メタデータのみ配布（音源は配らない）**。法的整理がコードと同型。J-POP薄→自前で度数化投入。
- 検証用に **Essen/民謡（権利クリーン）**。正規化＝調推定→度数列＋音程列、各音にコード文脈＋非和声音分類、多粒度索引（全体/フレーズ/2小節動機/(動機,コード)）。

## 6. 段階導入（縦スライス・各段は前段を壊さず property を足す）
- **S1（最優先・不満の核を直す）**：骨格層＝phrase/period＋**句末に休符/長音＋安定音着地**。既存反復・コードトーン拘束は維持。→「呼吸しない」が解消見込み。
- **S2**：後半≈0.62 頂点アーチ＋跳躍後の順次反行。
- **S3**：句機能で位置駆動の変奏（consequent=模続／句末=拡大・断片化）＋非和声音の役割化（順次解決保証）。
- **S4（連想の入口）**：骨格/エッセンス抽出＋**多層 melodySimilarity（音程＋リズム＋輪郭）**＋「このメロに似てる/かぶってない？」retrieval（メロ版 M2）。embeddings不要・既存DPの発展。
- **S5（北極星）**：エッセンス→違うメロ生成（G3/G4＋二重しきい値＋LCS上限）＋メロコーパス整備＋様式/アーティスト指紋。

## 7. 受け入れ基準（property test 案・既存 generate.test の流儀＝seed固定で musical ルールを担保）
1. 各 phrase 末に**休符 or 規定長以上の長音**が必ず存在（全曲が onset で埋まらない・period末≥phrase末）。
2. 終止が安定音着地：consequent/period 末音=主音(コードI上)、antecedent 末∈{属音,中音}。
3. 最高音(頂点)の位置 `start/total ∈ [0.5,0.75]`・頂点1〜2回・頂点後の平均ピッチ<前。
4. モチーフ反復が認識可能（dur列/コントゥア列が小節間で高一致・既存テスト拡張）。
5. 4度以上跳躍の直後は逆向き順次(≤4半音)が高割合・総音域≲1.5oct。
6. 非和声音は順次で入り順次で解決（孤立跳躍非和声音ゼロ・掛留は下行解決）。
7. mood連動：sparse の句末無音 > busy（既存「busy>sparse 音数」維持）。
8. 決定性：同 seed で完全一致。
9. （S4/S5）抽出骨格↔再生成メロ＝**エッセンス保持かつ表層は別物**／コピーゲートが LCS≥n を棄却。

## 8. 未確定・要ユーザー判断
- S1 のフレーズ単位の既定（2小節 phrase / 4小節 period でよいか・拍子別の扱い）。
- 著作権運用の最終ライン（保持する層・LCS上限 n・コーパス源の ToS 一次確認は実装直前）。
- S4/S5 の優先度（呼吸＝S1-3 を先に仕上げてから連想 S4-5 か、早めに連想の縦スライスも）。

参考出典は各パネルの調査ログに記載（Open Music Theory/Fiveable=楽節・動機、WuYun/GTTM=骨格、Pachet Markov-constraints/Strasheela=制約、Narmour=closure、Grachten/HLSD/Sturm=連想・コピー境界）。

---

# 深掘り（2026-06-23・骨格の難所）：実装サーベイ＋理論詰め

ユーザー方針＝「**類似実装だけ探す→その上で理論を詰められるだけ詰める**」。以下はその結果。

## 9. 実装ランドスケープ（何を借りるか・最重要発見）
**最重要発見：「コード外の音から和声音へ滑り込む」表情を*生成側で保証*している実装は CSP系(Strasheela / Cluster Engine)だけ**。ML系(WuYun/Magenta/DeepBach)は学習で暗黙化＝解決を保証しない、Markov/素朴生成器は何もしない（＝あてどない彷徨）。分析側(music21/Temperley)は判定はできる。→ **我々が明示規則で組めば、どの既製品より明示的に「滑り込み」を生成できる**（空白地帯＝勝てる）。

借りる物（ライセンス整理込み）：
| 何を | どこから | ライセンス | 流用形 |
|---|---|---|---|
| **表現＝度数×ローマ数字（"コード音か"は計算）** | Hooktheory/Hookpad | 思想(製品) | 旋律=度数(+oct+変化記号)、和音=ローマ数字、`inChord`は(度数,コード)から**計算する性質・格納しない**。「経過音＝単なる非コード度数」が自然に。既存C基準度数とも整合 |
| ピッチ演算基盤(五度座標で異名同音保持) | **Tonal.js** | **MIT/TS** | 直接流用可。Note(度数,dur,start)のイベント型は自前 |
| **拍強度の階層木（6/8で[1.0,.25,.25,.5,.25,.25]がただで出る）** | **music21** MeterSequence→beatStrength | BSD思想 | 拍子→(小節長, 拍群化simple/compound, 強拍位置と強度)を木で。加算拍子は Cluster の **alt-beatlength(小節を埋める拍長リスト)** で一般化(7/8=2+2+3) |
| **NCT解決の述語（滑り込みの核）** | **Strasheela の reified `inChordB`＋種カウンターポイント規則** | GPL思想 | 後付け修正でなく**構成上保証**：NCTは歩進で入り歩進で和声音へ抜ける時のみ合法。経過=同方向継続/刺繍=復帰/掛留=準備→強拍不協→**下行歩進解決**。Temperley の「解決が早いほど低コスト(SIOIの二次)」を重みに |
| **骨格→装飾の2段＋骨格抽出ヒューリスティクス** | **WuYun** | 思想(LICENSE無=コード不可) | 構造音＝①強拍メトリックアクセント②アゴーギク長音(隣接より長い)③シンコペ→の積/和。音高骨格＝数音セル毎に最も協和な音。「リズム骨格のみが交差より高品質＝過度に制約するな」 |
| **モチーフ変換カタログ＋"N小節前を反復"の一級化** | jMusic Mod(GPL思想) ＋ **Magenta lookback(Apache2)** | 一部コード可 | transpose/invert/retrograde(palindrome)/augment(elongate)/diminish(compress)/fragment。lookback-repeat ラベルで反復を一級シンボルに |
| **フレーズ境界＝別チャネルの旗** | DeepBach のフェルマータ条件付け | 思想 | 句境界をフラグで持ち、境界手前を和声音(カデンツ)へ強制＋息継ぎ挿入 |
| (将来)スタイル＋制約の厳密両立 | **FlowComposer の Markov制約**(DFA化→積→forward-backward BP) | 算法公開 | コーパス統計で"らしさ"＋「主音で終わる/パターン含む」を**バイアス無し**で。素朴な左→右フィルタは将来を無視＝必ず backward を計算 |

## 10. 理論で詰めた骨格文法（実装に接地）
### 10.1 表現（基盤決定）
旋律＝**スケール度数(+オクターブ軸+変化記号)**、和音＝度数/ローマ数字。`isChordTone(度数,コード)` は計算。これで NCT 文法(10.4)が綺麗に乗り、連想のエッセンス層(§3)とも同じ抽象度＝著作権セーフ。

### 10.2 拍子（6/8 を一級・パラメータ化）
拍子→`{barBeats, grouping(simple=2分割/compound=3分割), strongPositions, beatStrength[]}`。
- **4/4**：4拍・simple・強[1>3]。
- **6/8**：複合2拍子＝**付点四分が2ビート**、各3分割。beatStrength=[1.0,.25,.25,.5,.25,.25]。"うねり"＝オンセットを付点ビート頭＋各群の「長短(♩.→♪)」に寄せる。モチーフのリズム図形は**6/8ネイティブ**(付点四分/四分+八分/3連内分割)を用意。
- **3/4**：ワルツ・強[1>2,3]。
- 加算(7/8等)は alt-beatlength で後日。フレーズ末・カデンツ・息継ぎ位置は**拍強度木に従って**決める（句末は強拍に着地、息継ぎ＝次ダウンビート前の休符）。

### 10.3 弱起（拍子別・決定的）
- 入拍：4/4＝拍4 or 4裏／6/8＝最後の八分(6つ目) or 最後の付点ビート裏／3/4＝拍3。
- 音高：属音(5)・導音(7)・3度が多い＝ダウンビートのコードトーンへ**歩進 or 小跳躍で滑り込む**。
- 一貫性：弱起はモチーフ同一性の一部＝各句で保つ。**最終カデンツでは解消**（終止句はダウンビート着地で終える）。
- 機構：負 offset（既存エディタ/合成の pickup＝負start がそのまま）。

### 10.4 コード外から"滑り込む"文法（最難所・我々の発明点）
各音に計算 `inChord`。**強拍は既定コードトーン**だが、下記の*許可パターン*でのみ強拍に非和声音を置ける（必ず解決を伴う）：
- **倚音(appoggiatura)**：強拍へ**跳躍で入り**、**歩進(主に下行)で和声音へ解決**。許可場所＝句頭/頂点/盛り上げ＝「もたれ」の表情。
- **掛留(suspension)**：前コードの音を強拍へ保留→不協→**下行歩進解決**。強拍に不協を許す唯一の正攻法。
- **先取(anticipation)**：弱拍で次コードトーンを先取り。
弱拍の非和声音＝**経過(2コードトーン間を同方向歩進)** か **刺繍(歩進して戻る)**。**全NCTは歩進で入り歩進で和声音へ解決**＝孤立した跳躍非和声音ゼロ（解決保証＝CSP述語、生成時に強制）。
- 「**コードトーンの周りで踊る**」＝刺繍/経過で装飾しつつ次強拍の骨格音へ回帰。
- 「**滑り込む**」＝倚音入り＋導音/半音で下/上から和声音へ接近＋句頭が非和声音でダウンビートへ解決。
- **テンション(9/11/13/sus)**＝コード品質次第で"色"として強拍許容（解決不要の別枠）。
- **素直⇔表情ノブ**：強拍が許可NCTを取る頻度＋弱拍装飾量を1パラメータで（mood連動＋最終はユーザーの耳で較正）。

### 10.5 モチーフ→形式（1-2小節→2/4/8・2モチーフ）
- モチーフ＝1〜2小節（リズム図形＋度数コントゥア）。
- 2小節＝phrase（basic idea＋変奏/答え）。
- 4小節＝period(前楽節+後楽節) or sentence(提示2+継続/カデンツ2)。2モチーフ＝A+B or A+A'。
- 8小節＝AABA/AAB/二重period。A(主)＋B(対比)。
- 変奏は**句機能で位置駆動**（後楽節頭=模続、句末=拡大/断片化+カデンツ）。lookback で反復を明示。
- 大域アーチ：頂点≈0.62、上行接近→下行で閉じる。各句末＝息継ぎ＋安定音着地、最終句＝最強カデンツ(主音)。

### 10.6 カデンツ生成（実装が薄い＝理論で規定）
句境界フラグ→境界手前の音を**カデンツ目標へ強制**：前楽節末＝半終止感(5 or 2＝問い)、後楽節/最終＝完全終止(主音1＝答え)＋息継ぎ。

## 11. 既製品も埋めていない＝我々の発明点（理論で詰める核）
1. **"滑り込み"を能動的に*生成*する**（既製は判定/強制止まり）。10.4 の許可パターン＋解決保証を生成器に。
2. **6/8 × 非和声音の相互作用**（6/8を扱える実装は和声処理が弱い・逆も）。付点/複合拍のどの細分にNCTを置きどこで解決するか＝10.2+10.4 の合成。
3. **モチーフを保ったままの大域フレーズ弧**（全実装が弱い。Markovの彷徨はこの欠落）。10.5。
4. **カデンツの決定的生成**（検出は豊富・生成は希少）。10.6。

→ 次：この §10 文法を `docs/design.md` の生成設計に降ろす（実装前ゲート＝ユーザーレビュー後）。S1(呼吸＝10.2/10.3/10.6)から。著作権は §0.5/§3-4 のまま抽象のみ。
